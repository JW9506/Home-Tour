package fixtures;

public class OtherFixture extends Fixture {
  public OtherFixture(String name, String shortDescription, String longDescription) {
    super(name, shortDescription, longDescription);
  }
}
